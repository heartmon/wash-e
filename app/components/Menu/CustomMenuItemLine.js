import React from 'react';
import { View } from 'react-native';

const styles = {
  line: {
    backgroundColor: '#B9C5DE',
    height: 11,
    width: 160,
    marginTop: -24,
  }
}

const CustomMenuItemLine = () => (
  <View style={styles.line}>
    
  </View>
);

export default CustomMenuItemLine;
